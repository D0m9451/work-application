[package]
name = "Decoder"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12.20", features = ["blocking"] }
scraper = "0.18"
tokio = { version = "1", features = ["full"] }
